# SPEC.md — 「1分で世界を救え」仕様書(唯一のソース)

## このファイルについて
このプロジェクトの仕様は、このファイルに集約する。旧 `01-initial-ideas.md`(ブレスト経緯)・`02-final-spec.md`(確定仕様)は#46対応により本ファイルへ統合し削除した。

**運用ルール(CLAUDE.mdからも参照):**
- 仕様を新しく決めたとき・実装したときは、その都度このファイルを更新すること
- 仕様を把握する必要があるときは、このファイルを参照すること(履歴ファイルは復元しない)
- 「確定仕様」と「未確定・検討中の仕様」は章を分けて明示し、混同しないようにする
- 未確定事項について着手する際は、GitHub Issueと本ファイルの両方を更新する(Issueは個別タスクの追跡、本ファイルは常に現在の仕様のスナップショット)

---

## コンセプト
YouTube Shorts視聴の代替として遊ぶ、1分以内で完結するオートバトラーゲーム。
「運と才能」がテーマ。プレイヤーは操作せず、ガチャで決まった棒人間キャラが自動で世界を救う(または救えない)様子を見届ける。

- 形態: **単一HTML、縦画面(スマホ縦持ち想定、9:16目安)のWebサイト**
- ブラウザ拡張ではない。ブックマーク/URL直打ちでアクセスする緩い置き換え
- ホスティング: GitHub Pages(masaki0510名義、browser upload workflow)

### Shorts構造との対比(設計思想)
| Shorts | 本ゲーム |
|---|---|
| 無限に続く | 必ず終わる(勝利 or 敗北) |
| 次が来る快感 | ガチャの引きの快感 |
| 予測できるパターン | 毎回結果が変わる |
| だらだら見続ける | 最大1分で強制終了 |

### 採用に至った判断基準(検討経緯の要点)
- ブラウザ拡張による強制介入型・視聴時間の可視化による自己嫌悪利用型は却下(反発を生みやすい/継続性が低いと判断)
- Shortsの「無限に続く快感」ではなく「必ず終わる物語」であること
- ガチャの「引く楽しみ」は残しつつ、依存構造は再現しないこと
- 実力(演出)ではなく、結果を数値でガチ計算し、負けも運と才能の結果として受け入れる設計にすること
- 設計上の絶対原則: 「もう一回」を絶対に誘発しない/派手な演出より静かな達成感/Shorts的な無限リプレイ欲求を作らない

---

## 確定仕様(実装済み)

### ガチャ(開始時に確定する2軸)
- **才能**: 経験値倍率。0.5x〜3.0xの山型分布(三角分布)。高いほど道中のレベルアップが早い
- **運**: 装備ドロップ・クリティカル・レアモブ出現の重み付けに共通して使われる0〜1の値。高いほど上位枠を引きやすい
- どちらも見た目に反映(オーラ・装備アイコンの有無/グレード)し、開始直後に「今回の当たり外れ」が視覚的にわかるようにする

### ステージ進行
- 通常面: 3秒/面、6面(3ブロック × 各2面)
- 各ブロックの3面目に中ボス(5秒)、続けて大ボス(8秒)。これを3ブロック繰り返し、最後に魔王(10秒)
- 面順序: 通常→通常→中ボス→大ボス ×3ブロック → 魔王
- 想定トータル尺: 魔王撃破まで到達した場合で約50秒 → エンディング

### 戦闘・勝敗の判定方式(★最重要決定事項・変更不可の原則)
- **演出のために結果を調整しない。** DPS・敵HP・時間制限から純粋計算で勝敗を出す
- 才能(経験値倍率)と運(装備ランク・クリティカル)によって確定したステータスで、各面の制限時間内に敵を倒しきれるかを判定
- **倒しきれなかった面でそこで終了(敗北)。** 以降には進めない
- 「必ず魔王まで行ける」設計は不採用。行けないのも運と才能の結果として受け入れる
- クリティカルはヒット単位で毎回抽選し、実際に積み上がったダメージが制限時間内に敵HPへ届いたかどうかをその場で判定する(事前計算はしない)

### 敗北時のランク付け(収集要素)
実装当初は「通常面での敗北=大ハズレ」という想定だったが、#27の再検証で**通常面は才能・運の下限値でも理論上100%クリアできる**(dps floorが通常面の敵HPを常に上回る)ことが判明したため、以下のようにランクを再定義した:

| ランク | 到達条件 |
|---|---|
| 大ハズレ | 中ボス(いずれかのブロック)で敗北 |
| ハズレ | 大ボス(いずれかのブロック)で敗北 |
| 普通 | 魔王戦で敗北 |
| 激レア | 魔王撃破 |

#43で中盤(ブロック2・3の中ボス・大ボス)にもリスクを持たせたため、大ハズレ/ハズレとも「どのブロックで負けたか」は問わない(具体的な到達面はタイトル表示で動的に示す)。

自己ベストは `localStorage` に直近の最高ランク1件のみ保持する(全プレイ履歴のログは持たない。振り返り行為自体が新たな時間泥棒になるリスクを避けるため)。

### ドロップ・経験値
- 通常面・中ボス・大ボスいずれも撃破後にアイテムドロップ抽選(装備)
- ドロップ装備のランクは「運」の値に応じて出やすさが変わる(運が良いほど強い装備が出やすい)
- 敵撃破で経験値を獲得、経験値には「才能」の倍率がかかる
- 装備は現在ランクより上位のときのみ更新される(ダウングレードしない)

### レアモブ(#35で実装)
- 通常面(6面/run)でのみ、運の値に応じた重み付け抽選でレアモブが出現する(中ボス・大ボス・魔王は面ごとの個性を保つため対象外)
- **設計判断: HPは変えない「ボーナス敵」方式。** リスク/リターン型(レアモブを強くする)も検討したが、「★演出のための忖度をしない・純粋計算」の原則上、運良く出現した結果その面だけ不利になり得るのは運要素として筋が悪いと判断し、報酬のみ増える方式を採用
- 撃破時: 経験値 x2.5倍ボーナス、装備ドロップ抽選は運を一時的に+0.4上振れさせて判定
- 見た目: 金色オーラ + 専用ネームタグ(「レアモブ」)+ 出現時トースト演出

### 視覚デザイン
- 勇者: ドット絵アバター(base64 PNG埋め込み)。オーラの色/装備アイコン(🗡️⚔️👑)で装備ランクを表現
- 敵: 絵文字(通常👺 / 中ボス👹 / 大ボス🐗 / 魔王🐉)
- 背景(#34): 通常/中ボス/大ボスは`kusa.png`、魔王戦のみ`mao.png`。いずれも640px幅・JPEG品質72に圧縮しbase64インライン埋め込み(2枚合計約95KB)
- ステージ間は敵が右からスライドイン/左へスライドアウト(#13)。勝敗確定→結果画面はフェード遷移、クリア時は一瞬フラッシュ、敗北時は静かに暗転(#30)
- ヒット演出はダメージ数値(dmg-fx)・勇者の振りモーション・敵の被弾リアクションの3点に統一(#28で「!」ポップを廃止し重複を整理)

### パフォーマンス
- ヒット演出は`element.animate()`(Web Animations API)で実装し、強制リフローを発生させない(#29)

---

## 現在のバランス数値(仮値・実プレイで継続調整する前提)

`tools/balance_sim.js` でモンテカルロシミュレーション可能(`node tools/balance_sim.js`)。現在の主要パラメータ:

- 才能: 0.5x〜3.0x(三角分布)
- 装備ガチャ: コモン70% / レア20% / エピック8% / レジェンダリー2%(atkMul: 1.00 / 1.3 / 1.7 / 2.4)
- クリティカル: 通常70% / x1.2が20% / x1.5が7% / x2.0が2.5% / x5.0が0.5%
- 基礎攻撃力: 8、レベル毎+22%
- ヒット間隔: 通常260ms / 中ボス230ms / 大ボス200ms / 魔王170ms
- レアモブ出現率: 通常面ごとに運0で約10%〜運1で約24%、経験値x2.5・装備運+0.4ボーナス
- 中ボスHP: `45 + block*18 + 18*(block-1) + 6*(block-1)^2`(#43でブロック2・3を強化)
- 大ボスHP: `70 + block*35 + 34*(block-1) + 12*(block-1)^2`(同上)
- 魔王HP: 650(固定)

**30万回試行での現在の分布(2026-07-15時点、#43対応後):**
- 魔王撃破率: 約9.5%(目標5〜10%レンジ内)
- 大ハズレ(中ボス、3ブロック合計)敗北: 約28.1%(うち最初の中ボスが17.6%、ブロック2が3.1%、ブロック3が7.4%)
- ハズレ(大ボス、3ブロック合計)敗北: 約6.0%(ブロック1が0.25%、ブロック2が1.0%、ブロック3が4.8%)
- 普通(魔王戦)敗北: 約56.3%
- 平均レアモブ遭遇回数: 約0.91回/run(58%のrunが1回以上遭遇)

[Issue #44](https://github.com/masaki0510/1min-hero/issues/44)(魔王戦への結果一極集中)は#43対応後の上記分布で再検証した結果、クローズ済み。詳細は同Issueのコメントを参照(要点: 魔王HPをさらに下げると魔王撃破率が急上昇し目標レンジ〈5〜10%〉を割ってしまうことがシミュレーションで判明し、現状の「普通(56%)」は言葉通り最頻出という意味で妥当と判断した)。

---

## 未確定・検討中の事項

- **実機でのプレイフィール検証**([#45](https://github.com/masaki0510/1min-hero/issues/45)): #28/#29/#34で保留にした実機確認(フレームレート・視認性・回線速度体感)
- **収集要素の拡張範囲**: 装備の種類数・ランク差分の見た目実装は現状オーラ+アイコンのみ。拡張するかは未定
- **効果音**: 未実装・未検討

---

## 技術メモ
- 単一HTML、外部依存なし(スピログラフ・ブレイクと同方針)
- 縦画面固定レイアウトのみ考慮(横画面対応は不要)
- 画像アセットは全てbase64インライン埋め込み(`tmp/`配下の元画像はリポジトリには含めず作業用ファイルとして扱う)
- バランス調整は `tools/balance_sim.js`(Node実行、ゲーム本体には含まれない)で事前検証してから index.html に反映する
